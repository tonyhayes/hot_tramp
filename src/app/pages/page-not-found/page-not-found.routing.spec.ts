// Load the implementations that should be tested
import { routing } from './page-not-found.routing';
import {} from 'jasmine'

describe('page-not-found.routing', () => {

	it('should have a routing object',() => {
		expect(routing).toBeDefined();
	});

});