import { FormList } from './form.component';
import {} from 'jasmine'

describe('Form', () => {

	const dashboard = new FormList();
	//specs
	it('should create Form', () => {
		expect(dashboard).toBeDefined();
	});

}) 

