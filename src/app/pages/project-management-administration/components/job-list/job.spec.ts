import { JobList } from './job.component';
import {} from 'jasmine'

describe('JobList', () => {

	const dashboard = new JobList();
	//specs
	it('should create JobList', () => {
		expect(dashboard).toBeDefined();
	});

}) 

